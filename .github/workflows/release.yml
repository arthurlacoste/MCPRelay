name: Release Gate

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write

jobs:
  test:
    name: Python tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run tests
        run: pytest -q

      - name: Check whitespace errors
        run: git diff --check

  dedup:
    name: Duplicate code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Detect duplicate code
        run: npm run dedup

  release:
    name: Create GitHub Release
    needs:
      - test
      - dedup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Verify version matches tag
        shell: bash
        run: |
          set -euo pipefail
          test "v$(tr -d '\n' < VERSION)" = "${GITHUB_REF_NAME}"

      - name: Build release archive
        shell: bash
        run: |
          set -euo pipefail
          archive="gate-${GITHUB_REF_NAME}.tar.gz"
          git archive --format=tar.gz --prefix="Gate-${GITHUB_REF_NAME}/" -o "$archive" "$GITHUB_REF_NAME"
          sha256sum "$archive" > SHA256SUMS

      - name: Extract release notes from changelog
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          awk -v version="$version" '
            $0 == "## " version { found=1 }
            found && $0 ~ /^## / && $0 != "## " version { exit }
            found { print }
          ' CHANGELOG.md > release-notes.md
          test -s release-notes.md

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            "gate-${GITHUB_REF_NAME}.tar.gz" \
            SHA256SUMS \
            --verify-tag \
            --notes-file release-notes.md \
            --title "Gate $GITHUB_REF_NAME"
